<div class="container" >
  <% @foods.each do |food| %>
  <div class="card-product col-sm-12 m-2 d-flex justify-content-between">
  <div class="card-product-infos">
    <h2><%= food.name %></h2>
    <p><%= food.description %></p>
  </div>
    <div class="mr-2">
      <h2>Quantity</h2>
      <p class="fs-4 border border-3 rounded-4 p-1"> 4</p>
    </div>
  </div>

  <% end %>
</div>
